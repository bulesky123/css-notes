所有元素分为：块级元素，行内级元素
块级元素：包括：List-item   table  块元素(block) 都会站一行  

行内级元素：行内元素（inline） inline-block   inline-table    

行内元素又分为：替代元素（内置有宽高，可以设置宽高例如：img）                   非替代元素（不能设设置宽高，没有内置宽高）


padding:0px 10px 0px; /*上，左右，下*/


某个标记的来源：
1、浏览器默认样式（）
2、继承自祖先的样式
3、用户自定义样式

ul的padding不为零的环境

优先级的根源:

list-style-image:在IE6/7下有兼容问题

标准浏览器与非标准浏览器的差别：



块元素只是块级元素的一种，块级元素还包括table/ li中的默认样式 list-item等

块元素：只是display：block;

只要是显示为一行的都叫做块级元素
